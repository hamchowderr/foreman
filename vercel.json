{
  "framework": "nextjs",
  "installCommand": "rm -rf node_modules packages/*/node_modules && npm ci",
  "buildCommand": "cd packages/web && npm run build",
  "outputDirectory": "packages/web/.next"
}
